pip install -r requirements.txt
```

If you don't have `requirements.txt` yet, create it first:

---

## Create `requirements.txt`:
```
fastapi==0.109.0
uvicorn[standard]==0.27.0
httpx==0.26.0
pydantic==2.5.3
pydantic-settings==2.1.0
python-dotenv==1.0.0